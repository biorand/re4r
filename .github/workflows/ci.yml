name: CI
on: [push, pull_request]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        submodules: true
    - name: Restore
      run: dotnet restore src/biorand-re9
    - name: Build
      run: dotnet publish src/biorand-re9 -c release -r win-x64 -o out -p:PublishSingleFile=true
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: biorand4
        path: out/biorand-re9.exe
